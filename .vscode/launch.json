{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for XDebug",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "pathMappings": {
                "/webphp/core/vhosts/default": "${workspaceFolder}/core/vhosts/default"
            }
        }
    ]
}